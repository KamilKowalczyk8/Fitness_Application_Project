import 'package:flutter/material.dart';

class CoachMainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Trenerzy')),
      body: Center(child: Text('W fazie rozwoju')),
    );
  }
}
